name: Nightly Development Build

on:
    schedule:
      - cron: '0/5 * * * *'
    workflow_dispatch:
    
jobs:
    testing:
      runs-on: ubuntu-latest
      steps:
        - name: check schedule
          id: schedulecheck
          run: |
            echo "I am triggered by cron"
