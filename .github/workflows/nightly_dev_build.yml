name: Nightly Development Build
# mklomp/mirte-sd-image-tools/.github/workflows/buildFork.yaml@e72d96450aa76e57917731ff5ca38f298e0779f4

on:
    schedule:
      - cron: '0 2 * * *' # Will only run on main
    workflow_dispatch:
    
jobs:
    nightly-build:
       uses: mklomp/mirte-sd-image-tools/.github/workflows/buildFork.yaml@a1254863402f25eb4c3cab909a633c049dcb8a72
       with:
         branch: "develop"

